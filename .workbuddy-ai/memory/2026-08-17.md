# 2026-08-17 — MCP event server harness follow-up (Issue A/B/C + ACK coverage)

## What was done
- Completed the test-harness reliability follow-up for `D:\Temp\mcp-event` (production source frozen; no architecture redesign; deleted monoliths `test/test_phase8.py`, `test/integrate_test.py` NOT recreated).
- **Issue A** (`test/run_all.py`): runner now OWNS timeout cleanup via process-group signaling. Child spawned with `CREATE_NEW_PROCESS_GROUP` (Win) / `start_new_session=True` (POSIX). Graceful = `CTRL_BREAK_EVENT` to group (Win) / `SIGTERM` to group; hard = `taskkill /F /T /PID <child>` tree kill (Win, never `/IM python.exe`) / `SIGKILL` to group. Uses `proc.returncode is not None` (asyncio.subprocess.Process has no `.poll()`).
- **Issue B** (`run_all.py`): fully bounded `_GRACE_WAIT=10` + `_FINAL_WAIT=10`; distinct `TIMEOUT` status (not FAILED); `cleanup_result` diag; `[CLEANUP-FAILURE]` surfaced if STILL_ALIVE; narrow `(ProcessLookupError, OSError)` only; `KeyboardInterrupt`/`SystemExit` re-raised; 300 s/file retained.
- **Issue C** (`test/helpers/lifecycle.py`): `wait_mcp_ready` now mandatory = process-alive + TCP pre-check + REAL MCP op (`initialize()` + `call_tool("ping")`). TCP-open alone rejected. `ping_ok` set only after success (3.14 teardown ExceptionGroup ignored). `start_server` calls it with `timeout=20`.
- **Coverage** (`test/test_events.py`): added exactly ONE test `t12b_ack_advances_checkpoint` (register consumer → publish persistent event → `acknowledge_event` → checkpoint advances 0→seq); registered in `main()` test list.
- **Docs** (§35): README.md / AGENT.md / TEST_RUNTIME_MAP.md corrected — no longer claim `atexit` guarantees cleanup or that TCP-open == ready.

## Verifications (narrow, NOT full regression)
- `py_compile` of harness files: OK.
- `test_unit_sources.py`: 91 passed, 0 failed.
- `test/test_events.py`: 42 passed, 0 failed (exercises real readiness + t12b).
- Timeout smoke (throwaway, deleted): hung child starting a server.py → `TIMEOUT` @2.02 s, `CTRL_BREAK_EVENT->group; exited_within_grace`, no orphan server.py.
- Readiness smoke (prior session): TCP-open w/o MCP correctly rejected.

## IMPORTANT FINDING — §39 production-hash VIOLATION (action for user)
- `store.py` changed OUT-OF-BAND during the session (after my harness edits, without my instruction) from `577f3693…` to `3fa9928d…`. It is now a thin facade importing `store_modules.schema` (new `store_modules/` package). This is the `STORE_SERVER_SPLIT_PLAN.md` refactor. I did NOT author or touch it; harness unaffected (91/91 + 42/42 still pass). Reported in `HARNESS_FOLLOWUP_REPORT.md`. User must decide: keep refactor (refresh §39 baseline hash) or revert. Other 11 frozen prod files byte-identical.
- Deliverable: `D:\Temp\mcp-event\HARNESS_FOLLOWUP_REPORT.md` (sections A–H) → verdict `READY FOR FINAL HARNESS VERIFICATION` for the harness; store.py flagged separately.

## Environment note (cleanup)
- Safe-delete (genie-trash / Recycle Bin COM) FAILED on `.test_logs/` with Windows API error "The system call level is not correct"; Python delete guard also saturated at its per-turn threshold. `.test_logs/` (routine harness log dir, 46 files, NOT an orphan process) could not be deleted in-session — left as non-blocking artifact, cleanable manually or by a normal test run. Throwaway smoke scripts + `data_smoke/` were removed.
