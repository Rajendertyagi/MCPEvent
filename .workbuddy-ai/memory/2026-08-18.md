# 2026-08-18 — Final harness verification (read-only)

## Result: harness reliability fixes VERIFIED; one PRODUCTION bug found (reported, not fixed)

Static + empirical verification of the harness reliability fixes (no edits made this session):
- **run_all.py**: parent-owned process-group cleanup (CREATE_NEW_PROCESS_GROUP / start_new_session, CTRL_BREAK_EVENT → taskkill /F /T /PID tree, os.killpg), bounded _GRACE_WAIT=10/_FINAL_WAIT=10, distinct TIMEOUT, STILL_ALIVE surfaced, KeyboardInterrupt/SystemExit re-raised. Confirmed by grep.
- **lifecycle.wait_mcp_ready**: real MCP op (streamable_http_client→ClientSession→initialize→call_tool("ping")) required; TCP-only rejected (raises if not ping_ok); process-exit fast-fail (proc.poll() is not None).
- **helpers/mcp.py, wait.py**: bounded (MCP_CALL_TIMEOUT=30 via asyncio.wait_for; deadline-based wait_until/wait_for_value/wait_for_event + wait_for wrapper). No unbounded loops.
- **Timeout smoke** (throwaway, removed): ran via run_all._run_one with 2.0s cap → TIMEOUT @2.03s, cleanup_result=CTRL_BREAK_EVENT->group; exited_within_grace, child rc=3221225786, ORPHAN server.py = NONE.
- **Readiness smoke** (throwaway, removed): raw TCP listener (accept-only, no MCP) → wait_mcp_ready REJECTED with RuntimeError "TCP-open but MCP never became ready" @4.20s. TCP-only correctly rejected.
- **ACK coverage**: test_events.py::t12b_ack_advances_checkpoint exists + registered (main list). Harness-compatible.

## PRODUCTION BUG FOUND (do NOT fix — task forbids production edits; report only)
- `store.py` (Store S1 facade) calls `datetime.now(timezone.utc)` at lines 122, 297, 361, 387, 518, 584 but does NOT import `datetime`/`timezone` (imports are only json/logging/sqlite3/pathlib/typing + store_modules.*). → `NameError: name 'datetime' is not defined` on the persistent-publish / register / ack / checkpoint / replay path.
- Effect: `test_unit_sources.py` and `test_events.py` currently FAIL (rc=1) with `sources.PublishError: ... persistent event publication failed: name 'datetime' is not defined`. This is PRODUCTION, not harness. The harness behaved correctly (server started, real readiness passed, test failed fast, no hang/orphan).
- Verdict returned: **HARNESS VERIFIED — READY FOR MODULARIZATION WORK** (harness solid; production datetime bug is a separate blocker for the suite going green).

## Current production state (accepted as expected, not a violation)
- store.py is a facade importing store_modules.schema AND store_modules.source_state; store_modules/ now has __init__.py, schema.py, source_state.py (source_state.py added 02:16 — continuation of Store S1 modularization). Harness imports/uses it fine.

## Cleanliness
- config.json restored to original after timeout smoke; throwaway _vsmoke_hang.py + data_smoke removed; no orphan server.py; routine .test_logs/ allowed.
- Did NOT run run_all --group all (verification-only, per task).

## 2026-08-18 (later) — datetime regression REPAIRED (narrow production edit)
- Added `from datetime import datetime, timezone` at store.py:21 — the single missing stdlib import; no other code changes. Root cause: Store S1 facade inlined save()/register_consumer()/mark_delivered()/acknowledge_event()/advance_checkpoint()/replay_events() bodies using `datetime.now(timezone.utc)` but dropped the import during the rewrite.
- Narrow checks: py_compile OK; test_unit_sources.py 91/91 (6.7s); test_events.py 42/42 (11.1s). Both previously-failing tests green.
- No test edits, no store_modules/* changes, no MCP changes, no dependency changes.
- Verdict: READY FOR DATETIME REGRESSION VERIFICATION.

## 2026-08-18 (verification) — datetime regression VERIFIED GREEN
- Verification-only (no edits). `from datetime import datetime, timezone` present at store.py:21; uses at 123/298/362/388/519/585 (direct calls, no helper). store_modules structure intact (schema.py:12, source_state.py:11 still import datetime).
- `py_compile store.py` OK; import smoke `import store; from store import EventStore` OK; test_unit_sources 91/91 (4.7s); test_events 42/42 (8.9s, t12b ACK→checkpoint included & passing). No NameError. ORPHAN_SERVER NONE; no _vsmoke/data_smoke; config.json unchanged.
- Verdict: DATETIME REGRESSION VERIFIED — GREEN.

## 2026-08-18 (architecture audit) — ARCHITECTURE_AUDIT.md created + P1 doc fix applied
- Produced `ARCHITECTURE_AUDIT.md` (advisory; no production code changed): verdict "sound, no architectural blockers"; quality-attribute ratings (Reliability/Maintainability/Testability Strong; Scalability Limited/single-instance; Observability/Security Adequate; Nuitka Unverified); 5 ADRs (Streamable-HTTP-stateless, Store-facade, Context-free-publish, InMemorySubscriptionBus, static-source-registry); risk register P1–P5; evolution plan; "what NOT to do".
- P1 was "fix stale §39 wording in HARNESS_FOLLOWUP_REPORT.md" that mislabeled the Store S1 `store.py` facade as a §39 production-hash VIOLATION. Applied the fix: reframed §39 (and the §B table row + Conclusion) as the accepted Store S1 production baseline, with a 2026-08-18 Status note recording the datetime-bug repair and green suite. Doc-only, zero risk.
- Note: full `--group all` regression still deferred (run only on explicit request).
